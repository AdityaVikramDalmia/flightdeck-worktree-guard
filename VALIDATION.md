# Validation

Verified 2026-09-22: **36 regression tests passed on macOS and 36 on Linux**.

- macOS: Apple Silicon, Python 3.14.6; actual system Bash 3.2 entry points where applicable.
- Linux: unprivileged Alpine container, Python 3.14.7, Git 2.54.0, network disabled during tests.
- Command: `make test` in a standalone component tree. Linux uses a copied tree,
  a read-only container/source mount, and an executable temporary filesystem.
- No private agent configuration, accounts, model calls, or original rig services
  are required by these tests.

Runtime/test snapshot SHA-256: `fac872663fd2bb2d866893285530cbae08deaa0a2b3b854e20cd12bc868a6c5a`.
The digest covers sorted relative paths, content SHA-256, and executable bits for
runtime and test files plus Makefile; documentation and generated bytecode are excluded.

Independent review and failure-path tests are useful evidence, not a formal proof.
Read the documented limits before relying on the tool. Repeat `make test` after changes.
The companion `flightdeck-examples` repository includes the Linux validation harness.
